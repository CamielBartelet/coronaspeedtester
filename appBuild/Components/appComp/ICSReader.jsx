const ICS = () => {
 const fs = require('fs')
    fs.readFile('.../TestlocatieEindhoven.ics', 'utf-8', (err, data) => {
    if (err) throw err;
   // console.log(data);
})
return (
    data
);
};

export default ICS;